document.querySelectorAll('.choice-tool').forEach(tool=>tool.querySelectorAll('[data-choice]').forEach(button=>button.addEventListener('click',()=>{tool.querySelectorAll('[data-choice]').forEach(b=>b.setAttribute('aria-pressed',String(b===button)));tool.querySelector('.choice-result').textContent=button.dataset.choice;})));
document.querySelectorAll('.checklist').forEach(list=>list.addEventListener('change',()=>{const selected=[...list.querySelectorAll('input:checked')].map(i=>i.value);list.querySelector('.check-summary').textContent=selected.length?`${selected.length} selected: ${selected.join(' · ')}`:'Your list is ready to start.';}));
document.querySelector('.brightness')?.addEventListener('input',event=>{document.querySelector('.brightness-scene').style.filter=`brightness(${Number(event.target.value)/100})`;});
document.querySelectorAll('[data-color]').forEach(button=>button.addEventListener('click',()=>{document.querySelector('.paint-room').style.setProperty('--wall',button.dataset.color);document.querySelectorAll('[data-color]').forEach(b=>b.setAttribute('aria-pressed',String(b===button)));}));
document.querySelector('.room-planner input')?.addEventListener('input',event=>{const n=Number(event.target.value);document.querySelector('.room-planner p').textContent=n>=1&&n<=30?`${n} ${n===1?'room':'rooms'} on your cleaning plan. Ask the team for a quote.`:'Choose between 1 and 30 rooms to discuss.';});
document.querySelectorAll('.enquiry').forEach(form=>form.addEventListener('submit',event=>{event.preventDefault();form.querySelector('.form-result').textContent='Preview complete. Nothing was sent. To make a real enquiry, use the official business website.';}));
